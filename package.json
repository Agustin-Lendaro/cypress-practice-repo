{
  "name": "cypresstutorial",
  "version": "1.0.0",
  "description": "Elevate program Cypress tutorial",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "testall": "npx cypress run --spec cypress/e2e --headed --browser firefox"
  },
  "author": "AguL",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^14.4.1"
  },
  "dependencies": {
    "fs": "^0.0.1-security"
  }
}
